Original prompt: 当前目录有个泡泡游戏，你不管，不去看他的东西，然后AGENTS.md有个游戏开发指南说明。你给我造一个爆款的H5游戏，玩法闭环，要能达到生成级别上线，不管这个泡泡游戏，也不要类似游戏

## 目标与边界

- 新游戏独立位于 `zero_second/`，未读取或修改既有泡泡游戏。
- 单机、移动端优先兼容桌面、纯前端静态部署。
- 核心玩法：子弹时间瞄准 + 瞬移突袭 + 击破刷新 + 连击风险收益。

## 当前进度

- 已创建 Vite 项目骨架、响应式页面、标题/暂停/结算 UI。
- 已实现 6 个递进楼层、三种守卫、数据收集、撤离门、护盾、连击、计分、胜负与重开闭环。
- 已实现触摸/鼠标/键盘输入、子弹时间、冲刺路径与墙体阻挡、粒子/拖尾/震动/合成音效。
- 已实现 `render_game_to_text()` 与确定性 `advanceTime(ms)`。
- 首轮浏览器实测后优化教学层：核心交互现在可连续演示击破刷新链，不再要求首次空冲。
- 截图巡检修复桌面宽屏下主标题按视口宽度计算导致的断行。
- 移动端截图巡检将护盾信息移入目标状态条，避免与右上角声音/暂停控件重叠。

## 验证结果

- `npm run build`：通过；生产产物位于 `dist/`，JS gzip 约 8.07 kB，CSS gzip 约 1.85 kB。
- Playwright 指针链路：第一层可连续击破两名守卫、收集数据、开启撤离门并进入结算。
- 结算继续：可从第一层结算进入第二层，状态、生命和得分正确延续。
- 失败链路：守卫连续命中后进入 `gameOver`，生命归零且可重新开始。
- 暂停链路：暂停按钮进入 `paused`；键盘方向键 + Space 可完成瞄准与突袭。
- 真实触控事件：iPhone 13 模拟设备 `touchscreen.tap` 可击破目标并刷新冲刺。
- 截图检查：已检查移动端标题、移动端游戏内画面、桌面标题、完整楼层结算；修复标题断行和护盾控件重叠。
- 控制台与页面错误：上述场景均未发现错误。

## 已知边界与下一步

- 当前为纯前端单机版本，无账号、联网排行、广告、支付或埋点；这些均未获需求授权。
- 上线只需部署 `dist/`；若接入具体平台，再按平台要求补启动图、分享卡片和 SDK。

## 本机部署

- 已使用项目级 Nginx 配置部署，监听 `0.0.0.0:18080`，允许外网连接。
- `nginx -t` 通过；首页、JavaScript 和 CSS 均实测返回 HTTP 200。
- 首页使用 `no-cache`，带内容哈希的静态资源使用 7 天 immutable 缓存。
- 公网地址为 `http://64.83.41.39:18080`；已通过摩尔多瓦、土耳其、越南三个外部探测节点验证 HTTP 200。
- 本机 UFW 状态为不活动，无需增加防火墙规则。

## 2026-08-29 优化与扩展

- 修复受击击退绕过墙体碰撞导致玩家进入墙内并永久卡死的问题；冲刺和击退现在统一使用连续安全位移。
- 增加运行时玩家位置恢复不变量，以及墙边、墙角、多方向扫掠和真实炮塔击退回归测试。
- 增加 20 关数据、关卡实体合法性检查和所有必要目标的可达区域检查。
- 增加关卡选择、逐关解锁、版本化本地存档和三星评分；从任意已解锁关卡开始，失败后重试当前关。
- 三星规则：通关保底 1 星；有效移动次数不超过本关目标 +1 星；全程未受伤 +1 星。
- 增加护盾、EMP、医疗包。治疗不清除本关受伤次数，避免擦除评分代价。
- 20 关采用五组 `teach → develop → twist → test` 锯齿节奏，第 5/9/13/17 关主动降压后再爬升。

### 本轮验证

- `npm test`：物理、关卡数据、可达性、评分和存档共 7 项通过。
- 原始真实碰撞路径：炮塔连续两次向墙内击退玩家，最终 `playerOverlapsWall=false`。
- 浏览器逐一加载全部 20 关并进行多方向冲刺/受击压力检查，无入墙、无控制台错误。
- 第一关 4 次移动、0 受伤获得 3 星；刷新后记录保留并解锁第二关。
- 从第 19 关失败后重试仍进入第 19 关，生命与移动次数正确重置。
- 已检查移动端选关、三星结算、道具关卡和桌面选关截图。

## 2026-08-29 生产部署

- 重新执行 `npm test` 与 `npm run build`，7 项测试通过，生产构建成功。
- 已校验并重载项目级 Nginx，继续监听 `0.0.0.0:18080`。
- 本机与公网首页、JavaScript、CSS 均返回 HTTP 200；匈牙利、俄罗斯、土耳其三个外部节点回查均为 HTTP 200。
- 使用真实浏览器从 Nginx 进入第 1 关并完成移动；截图与 `render_game_to_text()` 状态一致，`playerOverlapsWall=false`，未发现控制台错误。

## 2026-08-29 关卡难度交叉评审

- 主代理与独立关卡评审 agent 分别按路线长度、敌方总血量、炮塔压力、猎手速度、墙体遮蔽和道具缓冲审查 20 关。
- 保留五组 `introduce → develop → twist → test` 的上升锯齿；章节切换的短暂降压用于安全教学，不改成机械式全程单调上升。
- 修正 F03 三星移动目标 `6 → 7`、F06 `7 → 8`；原目标依赖猎手追位或受墙体绕行影响，评分不够公平。
- F19 干扰器移到首段黄金路线 `[220,1040]`，缓和无遮蔽开局；F20 增加猎手 `[560,600]`，形成三类敌人综合终考。
- F20 的目标移动数 `11 → 12`、设计难度 `5 → 5.5`，确保终关成为全局峰值。
- 新增难度节奏护栏与 F20 十二步黄金路线回归；当前 `npm test` 共 9 项通过。
- 真实浏览器检查 F19/F20 画面和状态无控制台错误、无入墙；F20 已用动态瞄准在 12 次移动内实际通关。
- `npm run build` 通过，公网 Nginx 已提供新版 `index-n4yJai-a.js`，首页、脚本和样式均返回 HTTP 200。

### 最终发布复核

- 再次执行 `npm test` 与 `npm run build`：9 项测试全部通过，生产构建成功。
- Nginx 配置检查通过并完成重载，继续监听 `0.0.0.0:18080`。
- 从 Nginx 实际页面进入 F1 并完成移动，截图正常、状态一致、无控制台错误。
- 巴西、俄罗斯、乌克兰三个外部节点访问公网地址均返回 HTTP 200。

## 2026-08-30 撞墙伤害修复

- 复现确认：墙体本身不直接扣血；墙体截断冲刺后，玩家可能停在重甲/猎手接触范围内，下一帧被误判为敌人接触伤害。
- 修复：被墙体或边界截断的冲刺增加短暂碰撞保护，覆盖剩余冲刺冷却并保留 0.08 秒脱离窗口；撞墙仍保留移动计数、冷却和视觉反馈。
- 真实浏览器回归：F18 原始红灯路径在撞墙后立即及 650ms 后均保持 `health=3`、`hits=0`，玩家仍可再次行动；无控制台错误。

## 2026-08-31 撞墙状态根因修复

- 纠正上一版表面修复：墙体截断不再写入通用 `player.invuln`，因此不会触发玩家受击闪烁，也不会把墙碰撞伪装成无敌状态。
- 新增独立的 `wallContactGrace`，只在冲刺被墙体/边界截断时短暂抑制贴身敌人接触判定；子弹伤害、道具拾取、护盾和正常受击均沿用原逻辑。
- F18 原始复现路径：撞墙瞬间及 650ms 后均为 `health=3`、`hits=0`、`invulnerable=false`，冷却结束后可正常行动。
- F5 护盾拾取回归：`armor=1`、道具从场上消失、`invulnerable=false`；墙边道具逻辑未受影响。
- `npm test` 9 项通过，生产构建生成 `index-E3q5yO66.js`；真实浏览器截图正常且无控制台错误。

## 2026-08-31 道具接触误判修复

- 对照复现确认：F19 黄色干扰器拾取后 `health=3`、`hits=0`、得分增加 160，道具逻辑本身不会造成伤害；F9 黄色重甲接触后才会正常扣血。
- 检查全部关卡的道具与敌人出生位置，最近距离为 100px，不存在道具和敌人初始重叠导致的隐性接触伤害。
- 根因是视觉语义冲突：干扰器与重甲同为黄色，医疗包与敌人同为红色，且道具旋转形态接近危险单位，造成“碰黄色道具掉血”的感知误判。
- 统一视觉规则：奖励道具使用冷色、白色稳定六边形和脉冲拾取环；护盾为青色、干扰器为紫色、医疗包为绿色。敌人继续使用红/橙暖色、尖锐或旋转造型。
- `npm test` 9 项通过，`npm run build` 通过并生成 `index-CtvY0fVP.js`；已检查 F19/F20 道具截图和普通 F1 游戏流程，状态正常、无入墙、无控制台错误。

## 2026-08-31 F12 重甲不可通关修复

- 最小复现确认：第一次冲刺命中 F12 重甲时，重甲 `hp 2→1`，但玩家落在其贴身范围内，冷却期内会被重甲接触伤害，形成“正确攻击也必掉血”的不可通关路径。
- 根因修复：非致死重甲命中后，沿本次冲刺方向将玩家落点收回到敌人外侧，并通过 `safeCircleEndpoint` 再次做墙体/边界合法性裁剪；保留闲置状态接近敌人时的正常接触伤害。
- 新增物理回归：验证重甲命中后的落点与敌人保持 `enemy.r + player.r + 26px` 安全距离，且按重甲速度计算在完整冲刺冷却内无法追上。
- 真实浏览器 F12 回归：首次有效攻击保持 `health=3、hits=0`，重甲 `2→1`，玩家落点距离约 72px，无控制台错误；随后第二次攻击可在不受强制反伤的情况下击杀。
- `npm test` 10 项通过，`npm run build` 通过并生成 `index-B-0SuNP1.js`。

## 2026-08-31 纯手机触控收敛

- 移除键盘方向键、WASD、Enter/Space 冲刺、P/Escape 暂停、M 静音和 F 全屏快捷键；Canvas 现在只接受 `pointerType === 'touch'` 的指针输入。
- 保留并强化手机单指链路：按住进入瞄准、拖动预览、松手冲刺；暂停和声音仅通过屏幕右上角触控按钮操作。
- 删除桌面/键盘操作文案，状态接口改为明确描述 `touch only`，README 同步为纯手机 H5 说明。
- iPhone 13 模拟回归：触控拖拽期间 `aiming=true` 且移动次数不增加，松手后移动次数 +1；鼠标拖动移动次数保持不变；暂停/继续触控按钮正常；无控制台错误。
- `npm test` 10 项通过，`npm run build` 通过并生成 `index-VNmMf4MP.js`。
